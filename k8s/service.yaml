apiVersion: v1
kind: Service
metadata:
  name: focus-meditation-agent
  labels:
    app: focus-meditation-agent
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: focus-meditation-agent
  sessionAffinity: None
